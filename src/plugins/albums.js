// import { createPlugin } from "fusion-core";

// export default __NODE__ &&
//   createPlugin({
//     middleware() {
//       return (ctx, next) => {
//         if (ctx.method === "GET" && ctx.path === "/api/albums") {
//           ctx.body = { ok: 1 };
//         }
//         return next();
//       };
//     }
//   });
